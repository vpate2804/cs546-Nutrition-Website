const userData = require('./users');
const verifyData = require('./verify');
const recipeData = require('./recipes');

module.exports = {
    users: userData,
    verify: verifyData,
    recipes: recipeData
};
