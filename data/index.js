const userData = require('./users');
const verifyData = require('./verify');
const recipesData = require('./recipes');
module.exports = {
    users: userData,
    verify: verifyData,
    recipes: recipesData
};